#!/bin/bash

[[ -z "$1" ]] && { echo "Please specify a process name or number to look for"; exit 1; }

matcher="^[a-zA-Z0-9_~/.-]+$"

[[ "$1" =~ $matcher ]] && {
	top -n 1 -b -m -M | grep -E '(^\w|COMMAND|'$1'|^\s*$)'
}

