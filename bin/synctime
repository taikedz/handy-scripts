#!/bin/bash

NTPSERVICE=ntp
[[ -f /etc/redhat-release ]] && { NTPSERVICE=ntpd; }

service $NTPSERVICE stop
ntpdate -s ntp.ubuntu.com
service $NTPSERVICE start

# bonus: update timezone
#dpkg-reconfigure tzdata
