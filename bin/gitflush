#! /bin/bash

if [[ ! -e .git ]]; then
	echo "This is not a git repository..."
	exit 1
fi

echo "Comment is:"
echo " "
echo "    $@"
echo " "
read -p "Commit ? y/N> " ry

if [[ "x"$ry != 'xy' ]]; then
	exit 0
fi

git add .

git commit -m "$@"

if [[ $? = 0 ]]; then git push origin master; fi

