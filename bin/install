#!/bin/bash

# Add the most common items

if [[ "$UID" = 0 ]]; then
	mkdir -p /usr/local/bin/
	cp bin/af /usr/local/bin/
fi

mkdir -p "$HOME/bin"
cp bin/{secret,sendme,connect,launch,rotate-screen} "$HOME/bin/"



